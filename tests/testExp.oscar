#oscar;

main() {
  var
    int a, b, c;
    int d;
    float e, f;

  d = 1;
  a = 2;
  b = 3;
  f = 1.0;
  c = a + 14 + b;
  e = f - 3.141516;
  c = c * b + a;
  b = a * c;
  c = a % b + c;
  if ( a < b ){
    if ( a > b+c ){
      if ( a <= b/a ){
        if ( a != b%a ){
          a = b;
          print(a);
        } 
        else {
          b = a;
          print(b);
        }
      }
    }
    else {
      c =  1 + 2;
      print(c);
    }
  }
  e = 3.0 + 1 * 2;
  print(e);
}
