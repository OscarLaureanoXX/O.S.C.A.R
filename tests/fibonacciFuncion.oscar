#oscar;

def int fibonacci() {
  var
    int inicio = 0, actual = 1, suma = 0, limite; 
  
  for limite = read(int) : 15 {
    suma = inicio + actual;
    inicio = actual;
    actual = suma;
  }

  return suma;
}

main () {
  var
    int fib;
  fib = fibonacci();
  print(fib);
}