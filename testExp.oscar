#oscar;

main() {
  var
    int a, b, c;
    int d;
    float e, f;
    bool g;

  d = 1;
  g = _true;
  g = _false;
  c = a + 14 + b;
  e = f - 3.141516;
  c = c * b + a;
  b = a * c;
  c = a % b + c;
  if ( a > b ){
    if ( a >= b+c ){
      if ( a == b/a ){
        if ( a != b%a ){
          a = b;
        }
      }
    }
  }
} 